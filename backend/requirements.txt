fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
alembic==1.13.1
psycopg[binary]==3.2.1
cryptography==42.0.7
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.2.2
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (unmaintained since 2020) probes the bcrypt backend with a
# 73-byte password at import time; bcrypt>=4.1 raises instead of truncating,
# which breaks every hash_password/verify_password call. Pin bcrypt<4.1 until
# passlib is replaced or patched.
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# In-process, CPU-only embeddings (ONNX runtime, no torch) for
# BAAI/bge-small-en-v1.5 (see config.settings.embedding_model). Runs inside
# the FastAPI process itself - no separate Ollama/Chroma service needed.
fastembed==0.8.0

# Required by fastapi.testclient.TestClient (not otherwise a hard runtime
# dependency of the app).
httpx==0.27.0

pytest==8.2.2
pytest-asyncio==0.23.7
