fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
alembic==1.13.1
psycopg[binary]==3.2.1
cryptography==42.0.7
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.2.2
passlib[bcrypt]==1.7.4
# passlib 1.7.4 (unmaintained since 2020) probes the bcrypt backend with a
# 73-byte password at import time; bcrypt>=4.1 raises instead of truncating,
# which breaks every hash_password/verify_password call. Pin bcrypt<4.1 until
# passlib is replaced or patched.
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# NLP stack for the authorship engine. Versions are pinned together
# because they must all resolve to prebuilt wheels on linux aarch64
# (Oracle Ampere): thinc's latest release publishes none, and spaCy
# pins thinc>=8.3.12,<8.4.0, so 8.3.13 is the highest that works.
spacy==3.8.16
thinc==8.3.13
blis==1.3.3
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
# Spelling and syllable counting. pyphen is used directly rather than
# textstat, which drags in nltk and expects runtime corpus downloads.
pyspellchecker==0.9.0
pyphen==0.18.1
numpy>=1.26,<2

# Required by fastapi.testclient.TestClient (not otherwise a hard runtime
# dependency of the app).
httpx==0.27.0

pytest==8.2.2
pytest-asyncio==0.23.7
